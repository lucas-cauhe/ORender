## Render a scene
`dune exec render`

## Run the tonemapper
`dune exec tonemapper`

## Compare noise
`dune exec noise -- -in <noisy_img> -ref <reference_img>`

## Build docs
`dune build @doc`

## Run tests
`dune runtest`